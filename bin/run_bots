#!/usr/bin/env ruby
$LOAD_PATH << 'lib'

require 'dumb_bot'

continuous = ARGV[0] == 'c'

loop do
  puts "Running bots..."
  DumbBot.run
  if continuous
    sleep 2
    puts ''
  else
    exit 0
  end
end
